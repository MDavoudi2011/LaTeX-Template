% ==============================================================================
% main.tex
% ==============================================================================
\documentclass[12pt, a4paper, twoside]{book}

% 1. لود کردن پکیج‌های پایه و اصلی
\input{config/packages}

% 2. لود کردن تنظیمات کاربری (رنگ‌ها، حاشیه‌ها و اطلاعات سند)
\input{config/settings}

% 3. لود کردن استایل‌های سفارشی
\input{styles/layout}       % تنظیمات صفحه، سربرگ و فهرست
\input{styles/environments} % محیط‌های قضیه، نکته، مثال
\input{styles/cover}        % صفحه عنوان اختصاصی

% 4. لود کردن فونت‌ها و زی‌پرشین (بسیار مهم: همواره باید آخرین فایل لود شده باشد)
\input{config/fonts}

% ==============================================================================
% شروع سند
% ==============================================================================
\begin{document}

\maketitle

% شماره‌گذاری حروف ابجد/رومی برای صفحات اولیه
\frontmatter
\tableofcontents

% شروع شماره‌گذاری عددی صفحات اصلی
\mainmatter

\chapter{مقدمه و شروع به کار}
سلام! به قالب مدرن جزوه‌نویسی خوش آمدید. 
هدف از طراحی این ساختار، ایجاد بستری ماژولار، تمیز و بسیار حرفه‌ای است تا نوشتن متون علمی به سادگی \lr{Markdown} و با خروجی استاندارد \lr{LaTeX} انجام شود.

\section{تست فرمول‌ها و اعداد}
در این قالب از پکیج‌های پیشرفته ریاضی استفاده شده است. به عنوان مثال:
\begin{equation}
    \int_{-\infty}^{+\infty} e^{-x^2} dx = \sqrt{\pi}
\end{equation}
همانطور که می‌بینید، تنظیمات به گونه‌ای است که اعدادی مثل ۱، ۲ و ۳ در متن فارسی به درستی و با فونت اختصاص‌یافته نمایش داده می‌شوند.

\section{تست محیط‌های سفارشی}

\begin{definition}[هوش مصنوعی]
هوش مصنوعی (AI) به شاخه‌ای از علوم رایانه گفته می‌شود که هدف آن تولید ماشین‌های هوشمند است.
\end{definition}

\begin{theorem}[قضیه اساسی حساب دیفرانسیل]
فرض کنید $f$ تابعی پیوسته در بازه $[a, b]$ باشد. آنگاه...
\end{theorem}
\begin{proof}
این یک محیط اثبات بسیار تمیز است که در انتهای آن یک مربع توخالی قرار می‌گیرد.
\end{proof}

\newpage

\begin{example}
برای مثال، تابع $f(x) = x^2$ را در نظر بگیرید.
\end{example}
\begin{solution}
مشتق تابع برابر با $f'(x) = 2x$ خواهد بود.
\end{solution}

\begin{note}
شما می‌توانید به راحتی و بدون درگیر شدن با استایل‌های پیچیده، این باکس‌ها را ایجاد کنید.
\end{note}

\begin{warning}
فراموش نکنید که پکیج \lr{\texttt{xepersian}} باید همواره آخرین پکیجی باشد که در سند بارگذاری می‌شود!
\end{warning}

\section{ساختار پروژه}
این پروژه کاملاً ماژولار است. دایرکتوری‌های زیر برای مدیریت تمیز فایل‌ها ایجاد شده‌اند:
\begin{itemize}
    \item پوشه \lr{\texttt{config}}: تنظیمات (پکیج‌ها و فونت‌ها).
    \item پوشه \lr{\texttt{styles}}: استایل جعبه‌ها و محیط‌ها.
    \item پوشه \lr{\texttt{chapters}}: سورس متنی هر فصل.
    \item پوشه \lr{\texttt{figures}}: تصاویر ساختاریافته.
    \item پوشه \lr{\texttt{codes}}: سورس‌کدهای برنامه‌نویسی.
\end{itemize}

\subsection{تست زیربخش‌ها}
این یک زیربخش است که برای نمایش زیبایی خط رنگی زیر آن استفاده می‌شود. شما می‌توانید از این خطوط برای تفکیک بهتر مطالب در هر فصل استفاده کنید.

\end{document}
