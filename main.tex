\documentclass[12pt, a4paper, twoside]{book} % می‌توانید از article نیز استفاده کنید

\input{config/packages}
\input{config/settings}

\DocTitle{جزوه}
\DocSubtitle{نسخه تایپ شده و ساختاریافته}
\DocAuthor{محمد داودی}
\DocDate{\today}
\MarginType{narrow} % normal, narrow, wide

\input{styles/layout}       % تنظیمات صفحه، سربرگ و فهرست
\input{styles/environments} % محیط‌های قضیه، نکته، مثال
\input{styles/cover}        % صفحه عنوان اختصاصی

\input{config/fonts}

% ==============================================================================
% شروع سند
% ==============================================================================
\begin{document}

% شماره‌گذاری حروف ابجد/رومی برای صفحات اولیه
\frontmatter

\maketitle

\tableofcontents

% شروع شماره‌گذاری عددی صفحات اصلی
\mainmatter

\chapter{مقدمه و ارجاع‌دهی}
سلام! به قالب مدرن جزوه‌نویسی خوش آمدید. 
در این قالب سیستم ارجاع‌دهی هوشمند (\E{cleveref}) پیاده‌سازی شده است. به جای نوشتن دستی کلماتی مثل «بخش»، «شکل» یا «قضیه»، فقط کافیست از دستور \E{\textbackslash cref} استفاده کنید.

\section{تست فرمول‌ها و اعداد}
\label{sec:math}
در این قالب از پکیج‌های پیشرفته ریاضی استفاده شده است. به عنوان مثال:
\begin{equation}
    \label{eq:integral}
    \int_{-\infty}^{+\infty} e^{-x^2} dx = \sqrt{\pi}
\end{equation}
همانطور که در \cref{eq:integral} می‌بینید، تنظیمات به گونه‌ای است که اعداد در متن فارسی به درستی نمایش داده می‌شوند.

\section{تست محیط‌های سفارشی}

\begin{definition}[هوش مصنوعی]
\label{def:ai}
هوش مصنوعی (AI) به شاخه‌ای از علوم رایانه گفته می‌شود که هدف آن تولید ماشین‌های هوشمند است.
\end{definition}

\begin{theorem}[قضیه اساسی حساب دیفرانسیل]
\label{thm:fundamental}
فرض کنید $f$ تابعی پیوسته در بازه $[a, b]$ باشد. آنگاه...
\end{theorem}
\begin{proof}
این یک محیط اثبات بسیار تمیز است که در انتهای آن یک مربع توخالی قرار می‌گیرد.
\end{proof}

\begin{lemma}[لم مشتق‌گیری]
\label{lem:sample}
اگر تابعی در نقطه‌ای مشتق‌پذیر باشد، در آن نقطه پیوسته نیز هست.
\end{lemma}



\newpage

\begin{example}
\label{ex:sample}
همچنین همانطور که در \cref{def:ai}، \cref{thm:fundamental}، \cref{lem:sample} و \cref{ex:sample} مشخص است، سیستم شماره‌گذاری کاملاً یکپارچه و هوشمند است.
\end{example}
\begin{question}
تابع $f(x) = x^3 + 2x$ داده شده است. مشتق این تابع را به دست آورید و مقدار آن را در $x=1$ محاسبه کنید.
\end{question}
\begin{solution}
مشتق تابع برابر با $f'(x) = 3x^2 + 2$ خواهد بود. بنابراین مقدار آن در $x=1$ برابر است با ۵.
\end{solution}

\begin{note}
شما می‌توانید به راحتی و بدون درگیر شدن با استایل‌های پیچیده، این باکس‌ها را ایجاد کنید.
\end{note}

\begin{warning}
فراموش نکنید که پکیج \E{xepersian} باید همواره آخرین پکیجی باشد که در سند بارگذاری می‌شود!
\end{warning}

\section{ساختار پروژه}
این پروژه کاملاً ماژولار است. دایرکتوری‌های زیر برای مدیریت تمیز فایل‌ها ایجاد شده‌اند:
\begin{itemize}
    \item پوشه \E{config}: تنظیمات (پکیج‌ها و فونت‌ها).
    \item پوشه \E{styles}: استایل جعبه‌ها و محیط‌ها.
    \item پوشه \E{chapters}: سورس متنی هر فصل.
    \item پوشه \E{figures}: تصاویر ساختاریافته.
    \item پوشه \E{codes}: سورس‌کدهای برنامه‌نویسی.
\end{itemize}

\subsection{تست زیربخش‌ها}
این یک زیربخش است که برای نمایش زیبایی خط رنگی زیر آن استفاده می‌شود. شما می‌توانید از این خطوط برای تفکیک بهتر مطالب در هر فصل استفاده کنید.

\subsection{تست کدهای برنامه‌نویسی}
در اینجا یک کد پایتون به صورت درون‌خطی قرار داده شده است:
\begin{code}[python]{main.py}
def factorial(n):
    if n == 0:
        return 1
    else:
        return n * factorial(n-1)

print(factorial(5))
\end{code}

و در اینجا کد مستقیماً از فایل خوانده می‌شود:
\inputcode[python]{ch01.py}

\subsection{تست نمودار درختی}
شما می‌توانید ساختارها را با استفاده از محیط \E{moderntree} به سادگی رسم کنید:
\begin{moderntree}
[هوش مصنوعی
  [یادگیری ماشین
    [یادگیری نظارت‌شده]
    [یادگیری بدون نظارت]
  ]
  [پردازش زبان طبیعی
    [مدل‌های زبانی]
    [ترجمه ماشینی]
  ]
]
\end{moderntree}

همچنین محیط \E{mindmaptree} برای رسم نمودارهای بیضی‌شکل و مفهومی طراحی شده است:
\begin{mindmaptree}
[برنامه‌نویسی
  [فرانت‌اند
    [React]
    [Vue]
  ]
  [بک‌اند
    [Node.js]
    [Python]
  ]
]
\end{mindmaptree}

\end{document}
